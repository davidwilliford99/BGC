import React from 'react';


/**
 * 
 * For now, we will hard code this 
 * 
 * In the future, we will call the API to get the users 
 * 
 */

export default function CartPage(props) {
    return (
        <div>
            Cart
        </div>
    );
}